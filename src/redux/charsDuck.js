//const
const initialState = {
    chars : [],
}
const charReducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}
export default charReducer;